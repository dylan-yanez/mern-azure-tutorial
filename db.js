/* const Pool = require("pg").Pool;

const pool = new Pool({
    user: "postgres",
    password: "$323*xSdzEEE#FFF",
    port: "5432",
    database: "perntodo"
});

module.exports = pool; */ // Commented out because DB not in use